# AI Meeting Router 使用说明书

## 目录

1. [简介](#简介)
2. [环境要求](#环境要求)
3. [安装与启动](#安装与启动)
4. [页面说明](#页面说明)
5. [AI 配置指南](#ai-配置指南)
6. [启动讨论](#启动讨论)
7. [讨论结果与保存](#讨论结果与保存)
8. [置信度机制详解](#置信度机制详解)
9. [信誉引擎](#信誉引擎)
10. [常见问题](#常见问题)
11. [高级说明](#高级说明)
12. [故障排查](#故障排查)

## 简介

AI Meeting Router 是一个本地多智能体讨论平台，支持多个兼容 OpenAI API 的 AI 参与多轮讨论。系统使用 FastAPI 后端和原生页面前端，支持实时 SSE 流式输出、置信度判断、上下文压缩、AI 排序与自动自我介绍。

v3.0 新增了动态信誉引擎和增强的置信度校准机制，让多 AI 讨论的结果更加可靠。

## 环境要求

- Python 3.10 及以上
- Windows / macOS / Linux 均可
- 需要安装依赖包：`fastapi`、`uvicorn`、`jinja2`、`httpx`、`tiktoken`

## 安装与启动

### 1. 安装依赖

在项目根目录下运行：

```bash
pip install -r requirements.txt
```

### 2. 启动应用

```bash
python run.py
```

或：

```bash
python app.py
```

### 3. 打开浏览器

访问：

- `http://127.0.0.1:8000` - 讨论首页
- `http://127.0.0.1:8000/config` - AI 配置页
- `http://127.0.0.1:8000/discuss` - 讨论页
- `http://127.0.0.1:8000/docs` - API 文档

## 页面说明

### 讨论页面 `/discuss`

- 输入讨论问题
- 设置置信度阈值（默认 95）
- 设置最大轮数
- 点击"开始讨论"后实时展示 AI 发言
- 支持点击"停止"手动结束讨论

### AI 配置页面 `/config`

- 新增、编辑、删除 AI
- 设置 AI 名称、API 地址、API Key、模型、系统角色、上下文窗口
- 设置置信度校准值（范围 -100 到 100）
- 支持将某个 AI 标记为"压缩器模型"
- 保存后自动生成自我介绍，失败时可手动填写
- 支持拖拽排序

### 历史页面 `/history`

- 按时间倒序浏览所有讨论记录
- 查看完整的讨论过程和最终答案

## AI 配置指南

### 必填字段

- 名称：AI 的显示名称，例如"战略分析师"
- API 地址：兼容 OpenAI 的基础 URL，例如 `https://api.openai.com/v1`
- API Key：服务密钥
- 模型：例如 `gpt-4o`, `deepseek-v4-pro`, `qwen-plus` 等

### 可选字段

- 系统角色：用于定制 AI 行为
- 上下文窗口：默认 1000000
- 置信度校准：范围 -100 到 100，负值惩罚过度自信的 AI，正值提升保守 AI 的评分
- 作为压缩器模型：勾选后该 AI 作为摘要压缩专用模型
- 自我介绍：自动生成失败时手动填写

### 自我介绍说明

系统会自动请求 AI 生成自我介绍，格式为中文简短介绍，包含：
- 名字
- 擅长领域与能力
- 上下文窗口大小
- 工作风格或个性

若自动生成失败，页面会提示并保存当前配置，用户可手动补充自我介绍。

## 启动讨论

### 1. 输入问题

在讨论页面输入希望讨论的问题，例如："如何设计高可用分布式系统？"

### 2. 设置参数

- 置信度阈值：0-100，默认 95
- 最大轮数：每个 AI 最多回复轮数，默认 5

### 3. 开始讨论

点击"开始讨论"，系统会按照 AI 顺序轮流调用。

### 4. 讨论过程

界面会显示：
- 每个 AI 的开始事件
- 实时增量回复
- 置信度分数（原始值 + 校准值）
- AI 发言结束
- 最终答案与结束理由

### 5. 停止讨论

点击"停止"按钮可手动终止当前讨论。

## 讨论结果与保存

讨论结束后，最终答案会显示在页面底部。系统会自动将讨论记录保存为 JSON 文件，存储位置：

```
data/discussions/
```

讨论记录文件名为时间戳，例如：`20260510231752.json`。

## 置信度机制详解

### 校准提示词

v3.0 引入了结构化的置信度校准提示词，要求 AI 在输出置信度前执行 4 步自检：

1. 列出核心论点的 3 个最重要前提假设
2. 基于其他参与者的专长，指出谁最可能反驳哪个观点
3. 模拟该参与者会如何攻击论点的最薄弱环节
4. 基于模拟攻防，判断置信度是否需要下调

### 自动惩罚机制

系统会自动对以下行为进行惩罚：

- **空话惩罚**：发言未引用任何参与者且置信度 > 80，自动降至 60
- **高频警告**：同一 AI 历史发言置信度标准差 < 5 时，自动下调 5%-20%

### 收敛指导

当讨论达成合理共识时，提示词鼓励 AI 果断输出 FINAL_ANSWER，避免无休止的讨论。

## 信誉引擎

v3.0 新增的动态信誉引擎会根据 AI 在讨论中的客观行为自动更新信誉分数：

- 每个 AI 初始信誉分 50，范围 0-100
- 纯规则计算，无需额外 AI 调用
- 信誉分数影响最终答案的排序权重

信誉引擎帮助系统自动识别哪些 AI 在讨论中贡献更大，从而在选择最终答案时给予更合理的权重。

## 常见问题

### Q1: AI 自我介绍没有生成？
A: 请检查 API Key 与模型是否可用。若自动生成失败，页面会保存当前配置并允许手动补充自我介绍。

### Q2: 讨论超出上下文窗口？
A: 系统会自动压缩讨论历史，仅保留原始问题与参与者简介作为常驻信息。若配置了压缩器模型，将优先使用它进行摘要。

### Q3: 如何改变 AI 发言顺序？
A: 在配置页中拖拽 AI 卡片，系统会自动保存新的顺序。

### Q4: 讨论结果在哪儿保存？
A: 保存到 `data/discussions/` 目录下，文件名以时间戳命名。

### Q5: 置信度校准值怎么设置？
A: 在配置页的 AI 卡片中，找到"置信度校准"字段。如果某个 AI 总是过度自信（比如每次都打 90+），可以设负值（如 -10）来惩罚；如果某个 AI 太保守（总打 50 以下），可以设正值（如 +10）来提升。

### Q6: 讨论跑满了最大轮数还没结束？
A: 这说明没有任何 AI 的校准后置信度达到阈值。可以：降低置信度阈值（如从 95 降到 85），或增加最大轮数，或调整 AI 的校准值。

## 高级说明

### 压缩器模型
如果你希望使用单独的模型进行摘要，可将该 AI 勾选为"作为压缩器模型"。系统在压缩讨论历史时会优先调用该 AI 生成摘要。

### 二次摘要
当讨论历史继续增长时，系统可以对已生成的摘要再次压缩，避免上下文超限。

### Token 计数
系统会优先使用 `tiktoken` 进行精确 token 计数。若 `tiktoken` 不可用，则使用字符估算方式作为备用方案。

## 故障排查

### 1. 启动失败
- 请确认已安装依赖：`pip install -r requirements.txt`
- 确认 Python 版本为 3.10 或更高

### 2. 页面访问失败
- 检查是否已启动应用
- 确认访问地址为 `http://127.0.0.1:8000`

### 3. AI 请求失败
- 检查 `API 地址` 与 `API Key`
- 确认所选模型是否在服务端可用
- 检查网络是否正常

### 4. 讨论卡住或无输出
- 点击"停止"按钮终止
- 检查浏览器控制台是否有错误
- 检查后端终端日志

## 附录

### 常用命令

```bash
# 安装依赖
pip install -r requirements.txt

# 启动应用
python run.py

# 进入讨论页
http://127.0.0.1:8000/discuss

# 进入配置页
http://127.0.0.1:8000/config
```

### 目录结构

```
ai-meeting-router/
├── app/
│   ├── main.py
│   ├── models.py
│   ├── storage.py
│   ├── llm.py
│   ├── services/
│   │   ├── discussion.py
│   │   └── reputation.py
│   ├── templates/
│   │   ├── base.html
│   │   ├── config.html
│   │   ├── discuss.html
│   │   └── history.html
│   └── static/
│       ├── css/app.css
│       └── js/
│           ├── config.js
│           ├── discuss.js
│           └── history.js
├── data/
│   ├── ais_config.json
│   └── discussions/
├── app.py
├── run.py
├── requirements.txt
├── 使用说明书.md
└── QUICKSTART.md
```
