# AI Meeting Router — 开发日志

---

## v3.0 — 动态信誉引擎与置信度校准增强版

**发布日期**：2026-05-10
**基于版本**：v2.0

### 新增功能

1. **动态信誉引擎（Reputation Engine）**
   - 新增 `app/services/reputation.py`，纯规则计算，无需额外 AI 调用
   - 每个 AI 初始信誉分 50，范围 0-100
   - 根据 AI 的客观行为（置信度变化、是否引用他人、是否提出新角度）自动更新信誉
   - 信誉分数影响最终答案排序权重

2. **置信度校准增强**
   - 新增结构化校准提示词（`CONFIDENCE_CALIBRATION_PROMPT`），要求 AI 在输出置信度前执行 4 步自检：
     1. 列出核心论点的前提假设
     2. 指出最可能的反驳者和反驳点
     3. 模拟攻防
     4. 基于自检结果调整置信度
   - 新增收敛指导：当讨论达成合理共识时，鼓励果断输出 FINAL_ANSWER
   - 新增置信度含义对照表，避免 AI 凭感觉打分

3. **自动惩罚机制**
   - 空话惩罚：发言未引用任何参与者且置信度 > 80，自动降至 60
   - 高频警告：同一 AI 历史发言置信度标准差 < 5 时，自动下调 5%-20%
   - 用户配置的校准值（`confidence_calibration`）仍可叠加使用

4. **默认参数调整**
   - 默认置信度阈值从 80 调整为 95，鼓励更充分的讨论
   - 默认最大轮数从 3 调整为 5

### 修改文件清单

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `app/services/reputation.py` | 新增 | 动态信誉引擎完整实现 |
| `app/services/discussion.py` | 修改 | 集成信誉引擎、增强校准提示词、新增自动惩罚逻辑 |
| `app/models.py` | 修改 | 默认置信度阈值调整为 95 |

### 未变更文件

| 文件 | 说明 |
|------|------|
| `app/main.py` | 无改动 |
| `app/llm.py` | 无改动 |
| `app/storage.py` | 无改动 |
| `app/templates/` | 无改动 |
| `app/static/` | 无改动 |
| `requirements.txt` | 无改动 |
| `run.py` | 无改动 |
| `app.py` | 无改动 |

---

## v2.0 — 置信度校准版

**发布日期**：2026-05-03
**基于版本**：v1.0

### 新增功能

1. **置信度校准（confidence_calibration）**
   - 新增 `confidence_calibration` 字段，范围为 `[-100, 100]`，用于修正 AI 模型的置信度偏差
   - 负值可惩罚过度自信的 AI，正值可提升保守 AI 的评分
   - 校准值会加到 AI 输出的原始置信度上，再与用户设置的阈值进行比较

2. **校准后评分排序**
   - `_pick_best_answer()` 在最终答案选取和手动停止时，使用校准后的置信度进行排序
   - 阈值判断改为基于校准后的置信度（`calibrated_confidence`），逻辑更加合理

3. **前端展示优化**
   - 讨论页显示原始置信度 + 校准值 + 校准后置信度，一目了然
   - 配置页新增"置信度校准"表单字段，支持拖拽滑块和手动输入
   - AI 卡片列表展示校准值标签（如 `校准 +10` / `校准 -5`）

4. **系统提示优化**
   - 讨论规则中去掉冗余提示"注意最好让大多数参与者意见统一"，简化指令

### 修改文件清单

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `app/models.py` | 修改 | 新增 `confidence_calibration` 字段及校验器 |
| `app/services/discussion.py` | 修改 | 校准逻辑贯穿讨论服务：计算、展示、判断、排序 |
| `app/templates/config.html` | 修改 | 配置表单新增置信度校准输入控件 |
| `app/static/js/config.js` | 修改 | 前端读写 `confidence_calibration`，卡片渲染校准标签 |
| `app/static/js/discuss.js` | 修改 | 讨论页置信度展示包含校准信息 |

---

## v1.0 — 初始发行版

**发布日期**：2026-05
**说明**：项目首次发布。

### 核心功能

- 多 AI 智能体配置管理（名称、API 地址、模型、系统提示等）
- 多轮 SSE 流式讨论，支持置信度阈值自动结束
- 拖拽排序 AI 参与顺序
- 讨论历史持久化存储与回溯
- 上下文窗口自动压缩（TokenCounter）
- AI 自动生成自我介绍
- 响应式 Web UI（FastAPI + Jinja2 模板）
